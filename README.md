# Project Name: ACN Replication

## 🚀 Quick Start

### 1. Setup Environment
Ensure you have Python 3.x installed, then run:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
